<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>بطاقة الموظف</title>
<style>
body {
  display: flex;
  justify-content: center;
  align-items: center;
  background: #fff;
  height: 100vh;
  margin: 0;
}

.card.minimal {
  border: 2px solid #000;
  padding: 20px;
  text-align: center;
  font-family: monospace;
  width: 280px;
}

.card.minimal hr {
  margin: 15px 0;
}

.card.minimal img {
  margin-top: 10px;
}
</style>
</head>
<body>
  <div class="card minimal">
    <h2><%= employee.name %></h2>
    <small><%= employee.jobTitle %></small>
    <hr/>
    <p>☎ <%= employee.phone %></p>
    <p>✉ <%= employee.email %></p>
    <div class="qr">
      <img src="<%= employee.qrCode %>" alt="QR" width="100"/>
    </div>
  </div>
</body>
</html>
